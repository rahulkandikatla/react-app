import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Wrapper=styled.div`
${tw`bg-white`}
`;
const SizeFilterAndProductsList=styled.div`
${tw`flex `}
`;

export {Wrapper, SizeFilterAndProductsList}