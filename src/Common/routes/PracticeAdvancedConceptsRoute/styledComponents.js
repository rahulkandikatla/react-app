import styled from "@emotion/styled";
import tw from 'tailwind.macro';

const Wrapper=styled.div`${tw`flex flex-col items-center my-8`}`;
const Heading=styled.h1`
${tw`text-3xl font-bold mb-2`}
`;

export {Wrapper,Heading}